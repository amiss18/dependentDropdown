{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

    {#<h1>Edit Contact</h1>

    {{ include('contact/_form.html.twig', {'button_label': 'title.edit_contact' | trans( {'%id%': contact.id} ) }) }}


    {{ include('contact/_delete_form.html.twig') }}


    <div>
        <a class="btn btn-link" href="{{ path('contact_index') }}"> {{ 'action.back_to_list' | trans }}</a>
    </div>#}




{% block main %}

    <div class="card">
        <h5 class="card-header text-center">{{  'title.edit_contact' | trans }}</h5>
        <div class="card-body">
            {{ include('contact/_form.html.twig', {'button_label': 'title.edit_contact'  | trans }) }}


        </div>
    </div>

    <div>
        <a class="btn btn-link" href="{{ path('contact_index') }}"> {{ 'action.back_to_list' | trans }}</a>
    </div>


{% endblock %}


{% block sidebar %}
    {{ parent() }}

{% endblock %}


